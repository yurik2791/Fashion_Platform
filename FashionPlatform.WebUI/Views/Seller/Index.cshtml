@model IEnumerable<FashionPlatform.Domain.Entities.Product>

@*@{
    ViewBag.Title = "Admin Panel. List of Product";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}*@

<div class="panel panel-default">
    <div class="panel-heading">
        <h3>List of product</h3>
    </div>
    <div class="panel-body">
        <table class="table table-striped table-condensed table-bordered">
            <tr>
                <th>Name</th>
                <th class="text-right">Price</th>
                <th class="text-center">Action</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.ActionLink(item.Name, "Edit", new { item.ProductId })</td>
                    <td class="text-right">@item.Price.ToString("# $")</td>
                    <td class="text-center">
                        @using (Html.BeginForm("Delete", "Seller"))
                        {
                            @Html.Hidden("ProductId", item.ProductId)
                            <input type="submit" class="btn btn-default btn-xs" value="Delete" />
                        }
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="panel-footer">
        @Html.ActionLink("Create Item", "Create", null, new { @class = "btn btn-default" })
    </div>
</div>
